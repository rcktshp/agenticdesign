# Public URL of your deployed site (used for Stripe redirect URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3010

# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Local dev only: skip Stripe and use /download/success?session_id=dev_bypass
DEV_SKIP_DONATION=true
