# Offline book files

Place your generated exports here (not in `public/`):

- `agentic-design.pdf`
- `agentic-design.epub`

These paths are served only after Stripe verifies a donation meeting the minimum in `lib/site.ts`.
